export const defaultState = [];
export default function homeReducer(state,action){
    switch (action.type) {
        case 'SELECT_VALUE':
            break;
    
        default:
            break;
    }
}